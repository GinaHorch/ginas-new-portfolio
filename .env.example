# This file is a placeholder for environment variables required to run this project.
#
# The site currently needs no environment variables to build or run locally.
# Add any future variables here (without real values) so they are documented,
# and set the real values in .env.local (gitignored) and in the Vercel project.

# Example of what future variables might look like:
# NEXT_PUBLIC_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
