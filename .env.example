# This file is a placeholder for environment variables required to run this project.

# Password for the protected /agile/resource7 route (checked in src/pages/api/authenticate.ts).
# Set a real value in .env.local (gitignored) for local dev, and in the Vercel project's
# environment variables for production. Do not commit a real value here.
AGILE_RESOURCE_PASSWORD=

# Example of what future variables might look like:
# NEXT_PUBLIC_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here